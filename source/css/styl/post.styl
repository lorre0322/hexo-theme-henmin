.arti{
  margin: 20px 0;
  time{
    font-size: 14px;
  }
  time::before{
    content: "/";
    padding: 0 10px;
  }
}
.cont{
  h1::before{
    content: "#";
    color: var(--tx-se);
    padding-right: 10px;
  }
  a{
    color: var(--hl-key);
  }
}
.arti:first-of-type{
  margin: 10px 0 20px 0;
}